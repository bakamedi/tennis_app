import 'package:flutter/material.dart';

class ItemFieldW extends StatelessWidget {
  const ItemFieldW({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Expanded(
        child: PageView(
          children: [],
        ),
      ),
    );
  }
}
