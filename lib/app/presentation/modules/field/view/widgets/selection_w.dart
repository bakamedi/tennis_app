import 'package:flutter/material.dart';

class SelectionW extends StatelessWidget {
  const SelectionW({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Expanded(
        child: PageView(
          children: [],
        ),
      ),
    );
  }
}
